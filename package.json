{
  "name": "hotelflow",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  },
  "dependencies": {
    "google-auth-library": "^10.9.0",
    "googleapis": "^173.0.0",
    "mailparser": "^3.9.14"
  }
}
