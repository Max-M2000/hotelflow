{
  "name": "hotelflow",
  "private": true,
  "description": "Email to Ticket Automation for Hotels",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix . exec -- cp -r frontend/dist/* ."
  },
  "dependencies": {
    "google-auth-library": "^10.9.0",
    "googleapis": "^173.0.0",
    "mailparser": "^3.9.14"
  }
}
